# Patches

No upstream FFmpeg source files are modified for the Compress build.

The build is limited through configure flags only. If source changes are ever
introduced, place the corresponding patch file in this directory and mention it
from the release notes.
